import {Given,When,Then} from '@cucumber/cucumber'
import { testWorld } from '../world/customworld';

Given('Admin on the Dashboard Page after Login', async function (this: testWorld) {
  
});

Given('Admin navigate to Course Management Page', async function (this: testWorld) {
  
});

Given('Admin click add Course Structure for the {string}', async function (this: testWorld,corse:string) {
  
});

When('Admin clicks the Add Module icon', async function (this: testWorld) {
  
});

When('Admin enters the required module details', async function (this: testWorld) {
  
});

When('Admin clicks the Add Module button', async function (this: testWorld) {
  
});

Then('the newly added module should be displayed in the Module table', async function (this: testWorld) {
  
});
